# URL de l'API backend (utilisée par le frontend pour les appels API)
# Backend = port 3001, Frontend = port 3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL de l'app déployée sur Vercel (pour npm run vercel:open)
VERCEL_APP_URL=https://votre-projet.vercel.app
